[flake8]
# The virtualenv sits here when the backend is run locally, and linting a few
# thousand third-party files buries the handful of lines that are ours. CI has
# no .venv, so this only ever helps a developer.
exclude = .venv,venv,__pycache__,.git,build,dist
