# Runtime dependencies — everything the server actually imports, and nothing else.
#
# This file previously pinned 135 packages: pandas, numpy, boto3, openai,
# google-generativeai, huggingface_hub, tiktoken, tokenizers and friends, none
# of which appear in a single backend import. It also carried a private wheel
# URL, which is why .github/workflows/ci.yml skipped installing this file at all
# and could therefore only run static checks.
#
# Development, test and one-off tooling now lives in requirements-dev.txt.
# The original list is recoverable: git log -p -- backend/requirements.txt

# --- web framework ---
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
pydantic==2.13.4
email-validator==2.3.0        # required by pydantic's EmailStr

# --- database ---
motor==3.3.1
pymongo==4.5.0

# --- auth ---
bcrypt==4.1.3
PyJWT==2.13.0

# --- config ---
python-dotenv==1.2.2

# --- rate limiting ---
slowapi==0.1.10

# --- scheduling ---
APScheduler==3.11.2

# --- importers and crawlers ---
httpx==0.28.1
icalendar==7.1.2
beautifulsoup4==4.15.0
lxml==6.1.1                   # the parser BeautifulSoup is constructed with
python-dateutil==2.9.0.post0  # loose DE/FR date parsing in importers.py

# --- OpenStreetMap POI ingest ---
osmium==4.3.1

# --- payments ---
stripe==14.4.1
